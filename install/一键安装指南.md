# 🚀 VSCode 扩展一键安装指南

提供两种版本的安装脚本，满足不同用户的需求：

## 📦 两种版本选择

### 🌟 完整版（自动安装 VS Code + 扩展）

**适合：还没有安装 VS Code 的用户**

- ✅ **自动检测 VS Code 是否已安装**
- ✅ **如果未安装，自动下载并安装 VS Code**（Windows/macOS/Linux 全支持）
- ✅ **自动添加到 PATH**（无需手动配置）
- ✅ **自动安装 VibeVideo 和 Qwen Code Companion 扩展**

**文件名**：
- Windows: `install-vscode-extensions-full.bat` / `install-vscode-extensions-full.ps1`
- macOS/Linux: `install-vscode-extensions-full.sh`

> ⚠️ **注意**：如果您同时安装了 Cursor 编辑器，完整版脚本可能会失败，请使用精简版。

### 🎯 精简版（只安装扩展）

**适合：已经安装了 VS Code 的用户**

- ✅ **需要先手动安装 VS Code**
- ✅ **自动安装 VibeVideo 和 Qwen Code Companion 扩展**
- ✅ **更稳定，不会因编辑器冲突而失败**

**文件名**：
- Windows: `install-extensions-only.bat` / `install-extensions-only.ps1`
- macOS/Linux: `install-extensions-only.sh`

## 📥 可选：手动安装 VS Code

如果您想手动安装 VS Code，可以参考以下步骤：

### Windows 用户

1. **访问 VS Code 官网**：https://code.visualstudio.com/
2. **下载安装程序**：点击"Download for Windows"按钮
3. **运行安装程序**：
   - 运行下载的 `.exe` 文件
   - **重要**：在安装选项中，务必勾选以下选项：
     - ✅ **"添加到 PATH"**（Add to PATH）- 这个选项必须勾选！
     - ✅ **"创建桌面快捷方式"**（Create desktop shortcut）- 可选但推荐
4. **完成安装**：点击"安装"并等待完成

### macOS 用户

1. **访问 VS Code 官网**：https://code.visualstudio.com/
2. **下载安装包**：点击"Download for Mac"按钮
3. **安装 VS Code**：
   - 下载完成后，打开 `.dmg` 文件
   - 将 VS Code 拖拽到 Applications 文件夹
4. **添加命令行工具到 PATH**（重要）：
   - 打开 VS Code
   - 按 `Cmd + Shift + P` 打开命令面板
   - 输入并选择：`Shell Command: Install code command in PATH`
   - 这将允许在终端中使用 `code` 命令

### Linux 用户

1. **访问 VS Code 官网**：https://code.visualstudio.com/
2. **下载安装包**：根据您的发行版选择对应的安装包（.deb 或 .rpm）
3. **安装 VS Code**：
   
   **Debian/Ubuntu:**
   ```bash
   sudo dpkg -i code_*.deb
   sudo apt-get install -f
   ```
   
   **Fedora/RHEL:**
   ```bash
   sudo rpm -ivh code-*.rpm
   ```
4. **验证安装**：打开终端，输入 `code --version`，如果显示版本号说明安装成功

## 🚀 使用一键安装脚本

### 选择哪个版本？

**使用完整版，如果：**
- ✅ 还没有安装 VS Code
- ✅ 想一键完成所有安装
- ✅ 不介意自动下载安装 VS Code

**使用精简版，如果：**
- ✅ 已经安装了 VS Code
- ✅ 同时安装了 Cursor 编辑器（完整版可能会失败）
- ✅ 想更快地只安装扩展
- ✅ 想自己控制 VS Code 的安装过程

### Windows 用户

#### 完整版（自动安装 VS Code + 扩展）

**方式一：批处理脚本（推荐新手）**
1. **双击运行** `install-vscode-extensions-full.bat`
2. 等待脚本自动下载安装 VS Code（如果未安装）
3. 等待脚本自动安装扩展
4. 完成后重启 VS Code

**方式二：PowerShell 脚本（推荐）**
1. **右键点击** `install-vscode-extensions-full.ps1`
2. 选择"使用 PowerShell 运行"
3. 等待脚本自动完成所有安装
4. 完成后重启 VS Code

#### 精简版（只安装扩展）

**方式一：批处理脚本**
1. **双击运行** `install-extensions-only.bat`
2. 等待脚本自动安装扩展
3. 完成后重启 VS Code

**方式二：PowerShell 脚本**
1. **右键点击** `install-extensions-only.ps1`
2. 选择"使用 PowerShell 运行"
3. 等待脚本安装扩展
4. 完成后重启 VS Code

**提示**：建议以管理员身份运行脚本（右键 → 以管理员身份运行）

### macOS / Linux 用户

#### 完整版（自动安装 VS Code + 扩展）

1. **打开终端**，进入 `install` 文件夹
2. **添加执行权限**：
   ```bash
   chmod +x install-vscode-extensions-full.sh
   ```
3. **运行脚本**：
   ```bash
   ./install-vscode-extensions-full.sh
   ```
4. 等待脚本自动下载安装 VS Code（如果未安装）
5. 等待脚本自动安装扩展
6. 完成后重启 VS Code

#### 精简版（只安装扩展）

1. **打开终端**，进入 `install` 文件夹
2. **添加执行权限**：
   ```bash
   chmod +x install-extensions-only.sh
   ```
3. **运行脚本**：
   ```bash
   ./install-extensions-only.sh
   ```
4. 等待脚本自动安装扩展
5. 完成后重启 VS Code

**提示**：可能需要输入管理员密码来安装 VS Code（仅完整版）

## ⚠️ 重要提示

### 完整版提示

- ✅ **网络连接正常**：脚本需要下载 VS Code 和扩展（Windows 约 150MB，macOS/Linux 约 200MB）
- ✅ **Windows 用户**：建议以管理员身份运行脚本（可选但推荐，确保安装成功）
- ✅ **macOS/Linux 用户**：可能需要输入管理员密码来安装 VS Code
- ✅ **如果 VS Code 已安装**：脚本会跳过下载，直接安装扩展
- ⚠️ **如果同时安装了 Cursor 编辑器**：完整版脚本可能会失败，请使用精简版

### 精简版提示

- ✅ **必须先安装 VS Code**：请先手动安装 VS Code 并添加到 PATH
- ✅ **网络连接正常**：脚本需要从扩展市场下载扩展
- ✅ **VS Code 必须添加到 PATH**：确保可以在终端使用 `code` 命令

## 📦 脚本将安装的扩展

1. **VibeVideo** (`fastpen.vibevideo`)
   - 像写代码一样创建视频
   - 使用 Markdown 编写脚本，AI 自动生成视频

2. **Qwen Code Companion** (`Qwen.qwen-code-companion`)
   - 阿里云通义千问 AI 编程助手
   - 提供智能代码补全和AI对话

## 🖥️ 支持的操作系统

- ✅ **Windows** (Windows 10/11)
- ✅ **macOS** (macOS 10.14+)
- ✅ **Linux** (Ubuntu, Debian, Fedora, 等主流发行版)

## 🎯 使用场景

这些脚本特别适合：

- 初次使用 VS Code 的用户
- 不熟悉扩展安装流程的用户
- 需要批量配置开发环境的用户
- 团队环境统一配置
- 跨平台开发环境的快速配置

## ❓ 遇到问题？

请查看 `一键安装说明.md` 获取详细的故障排除指南。

---

**提示**: 脚本会自动检测 VS Code 是否已安装，如果未安装会提供清晰的安装指引。

