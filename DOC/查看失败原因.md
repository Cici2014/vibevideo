# 🔍 查看任务失败原因

## ✅ 已改进

我刚刚改进了代码，现在会显示**详细的失败原因**！

---

## 🚀 重新测试

### 步骤
1. **重新启动扩展**
   ```
   在原始窗口按 Shift+F5 停止
   然后按 F5 重新启动
   ```

2. **再次运行命令**
   ```
   在扩展开发主机窗口：
   Ctrl+Shift+P → "Vibe Video: Generate First Frames"
   ```

3. **查看调试控制台**
   ```
   在原始窗口（vibevideo 项目）：
   查看 → 调试控制台
   ```

### 现在会看到
```
[API] 任务状态响应: {
  task_status: 'FAILED',
  output_code: 'ContentModeration.Blocked',  ← 失败代码
  output_message: '内容包含敏感信息',       ← 失败原因
  full_output: { ... }                      ← 完整响应
}

[轮询] ✗ 任务失败: 内容包含敏感信息

生成首帧失败: 01-opening 生成失败: 内容包含敏感信息
```

---

## 📊 从日志分析

### 您之前的日志显示
```
[API] 任务状态响应: { task_status: 'FAILED', code: undefined }
```

**问题**：
- 任务创建成功 ✅
- 但服务端执行失败 ✗
- 没有显示失败原因

**现在会显示**：
- `output_message`: 失败的详细原因
- `output_code`: 失败的错误代码

---

## 🤔 可能的失败原因

### 1. 内容审核失败（最可能）⚠️
```
错误代码：ContentModeration.*
原因：提示词包含敏感内容
```

您的提示词包含：
- "诸葛亮"、"周瑜"、"曹操"（历史人物）
- "军营"、"弓箭手"、"箭矢"（军事内容）

**解决方案**：
- 避免直接使用真实历史人物名字
- 改为"古代军师"、"将领"等通用描述
- 避免暴力、军事相关内容

### 2. 参数格式问题
```
错误代码：InvalidParameter
原因：size 格式不对
```

可能是：
- `1280x720`（我们用的）
- `1280*720`（API 要求的）

**解决方案**：
检查 size 参数格式

### 3. 模型不可用
```
错误代码：ModelNotAvailable
原因：模型服务异常
```

**解决方案**：
稍后重试或联系阿里云

### 4. 额度不足
```
错误代码：InsufficientBalance
原因：余额不足
```

**解决方案**：
充值或申请免费额度

---

## 🎯 请重新测试并告诉我

重新运行后，请提供：

1. **完整的错误信息**
   ```
   [API] 任务状态响应: {
     output_code: '这里是什么？',
     output_message: '这里是什么？'
   }
   ```

2. **错误弹窗内容**
   应该会显示："生成失败: XXX详细原因"

有了详细原因，我就能针对性地解决问题！🎯

---

## 💡 预判：可能是内容审核

根据您的提示词（三国故事，包含历史人物和军事内容），**很可能是内容审核失败**。

如果确实是这个原因，我们需要：
1. 修改提示词，避免敏感内容
2. 或联系阿里云申请白名单
3. 或使用更通用的描述

**请重新测试并告诉我 `output_message` 的内容！**

