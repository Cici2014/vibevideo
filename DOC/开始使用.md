# 🎉 Vibe Video - 开始使用

## 当前状态

### ✅ 已完成
- **阶段 1**：核心脚手架（100%）
- **阶段 1.5**：API 架构（90%）

### 🎯 总进度
**架构完成度：95%** - 只差真实 SDK 集成！

---

## 🚀 立即开始测试

### 第 1 步：启动扩展（5秒）

**在当前项目窗口（vibevideo）中：**
```
按 F5 键
```

会自动编译并打开一个新窗口（标题显示"[扩展开发主机]"）

---

### 第 2 步：创建测试项目（1分钟）

**在新窗口中：**

1. 打开文件夹：
   ```
   Ctrl+K Ctrl+O
   ```

2. 选择或创建一个测试文件夹：
   ```
   例如：E:\mylab\test\MyFirstVideo
   ```

3. 初始化项目：
   ```
   Ctrl+Shift+P → 输入 "init" → 选择 "Vibe Video: Initialize Project"
   ```

4. 观察结果：
   - ✅ 自动创建所有文件夹
   - ✅ 生成 .cursorrules、剧本.md 等
   - ✅ 自动打开剧本.md

---

### 第 3 步：生成分镜（2分钟）⭐ 关键测试

**这是最重要的测试！验证 Cursor AI 集成是否成功。**

1. 编辑 `剧本.md`（或使用默认内容）

2. 打开 Cursor AI：
   ```
   Ctrl+L
   ```

3. 输入：
   ```
   根据剧本.md 生成分镜脚本
   ```

4. 等待 AI 生成...

5. **检查关键点**：
   - ✅ AI 是否读取了 .cursorrules？
   - ✅ 生成的是 `.md` 文件还是 `.json` 文件？（必须是 .md！）
   - ✅ 文件保存在 `storyboards/` 目录？
   - ✅ 内容格式正确？

**示例生成的文件** (`storyboards/01-opening.md`):
```markdown
# 开场镜头

- **时长**: 5秒

城市清晨的天际线，玻璃幕墙的高楼反射着金色的阳光。
无人机视角从低空缓慢上升，展现整个城市轮廓。
街道上车流如织，人们开始一天的忙碌。
光线温暖明亮，画面充满希望和活力的感觉。
```

---

### 第 4 步：查看资源（1分钟）

1. 点击左侧活动栏的 📹 Vibe Video 图标

2. 应该看到：
   ```
   📝 分镜脚本 (3)
   ├── ✅ 01-opening.md (优秀)
   ├── ✅ 02-scene.md
   └── ⚠️ 03-outro.md (描述较短)
   ```

3. 点击任意分镜 → 在编辑器中打开

---

### 第 5 步：测试质量检查（30秒）

```
Ctrl+Shift+P → "check" → "Vibe Video: Check Storyboards Quality"
```

会打开输出面板，显示详细的质量报告。

---

### 第 6 步：测试 API 配置（1分钟）⭐ 更友好

#### 方式 1：使用命令
```
Ctrl+Shift+P → "config" → "Vibe Video: Configure Video AI"
```

会显示对话框，点击"打开设置"

#### 方式 2：直接打开设置（推荐）
```
Ctrl+, → 搜索 "vibevideo"
```

或者：
```
文件 → 首选项 → 设置 → 扩展 → Vibe Video
```

**在设置页面中**：

1. **Tongyi: Access Key Id** - 输入您的 Access Key（测试可输入：`LTAI5tTest123456789`）
2. **Tongyi: Access Key Secret** - 输入 Secret（测试随意输入）
3. **Video: Resolution** - 选择分辨率（默认 1280x720）
4. **Video: Default Duration** - 设置默认时长（默认 5 秒）
5. 配置自动保存！

#### 查看配置
```
Ctrl+Shift+P → "Vibe Video: Show Current Config"
```

显示配置信息，并可点击"打开设置"快速修改。

---

### 第 7 步：测试生成命令（1分钟）

#### 测试生成初始帧
```
Ctrl+Shift+P → "Vibe Video: Generate First Frames"
```

- 如果分镜没有 `firstFramePrompt`，会提示"不需要生成"
- 如果有，会显示确认对话框，然后报错"尚未实现"（预期）

#### 测试生成视频
```
Ctrl+Shift+P → "Vibe Video: Generate All Videos"
```

- 会读取所有分镜
- 显示确认对话框（"将生成 N 个视频..."）
- 点击"继续"后报错"尚未实现"（预期）

**重点**：流程是否顺畅？错误提示是否友好？

---

## ✅ 测试检查清单

**基础功能（必测）**：
```
□ 1. F5 启动成功
□ 2. 打开测试文件夹
□ 3. 初始化项目 → 文件创建
□ 4. .cursorrules 内容正确（提到"Markdown"）
□ 5. Cursor AI 生成分镜
□ 6. 生成的是 .md 文件（不是 .json）✨ 关键！
□ 7. 侧边栏显示分镜
□ 8. 点击能打开文件
□ 9. 质量检查工作
```

**API 功能（新增，设置面板方式）**：
```
□ 10. Ctrl+, 打开设置
□ 11. 搜索 "vibevideo" 找到配置项
□ 12. 输入测试 Access Key
□ 13. Show Current Config 显示正确
□ 14. 生成命令触发正确（虽然会报错）
```

**如果前 9 个成功 → 基础功能通过！**  
**如果第 10-14 个流程正确 → API 架构完成！**

**配置更简单了**：直接在设置面板中输入，更符合 VS Code 规范！✨

---

## 🎊 成果展示

### 您现在拥有：

1. **完整的项目架构** ✅
   - 13 个模块，结构清晰
   - 类型安全，代码质量高

2. **8 个可用命令** ✅
   - 项目管理 4 个
   - API 功能 4 个

3. **Markdown 工作流** ✅
   - 不用 JSON，更简单
   - AI 天然理解
   - Git 友好

4. **完整的文档** ✅
   - 10+ 篇设计和实施文档
   - 详细的测试指南
   - SDK 集成说明

5. **可扩展架构** ✅
   - Provider 接口
   - 未来可轻松添加 Runway、Pika 等

---

## 🤔 接下来做什么？

### 立即行动
1. **按 F5 测试**（最重要！）
   - 验证基础功能
   - 测试 Cursor AI 集成
   - 确认架构无误

2. **决定下一步方向**：
   - 集成真实 SDK？
   - 创建 Mock Provider？
   - 继续其他功能？

### 如果测试通过
说明：
- ✅ 架构设计正确
- ✅ 实现质量高
- ✅ 可以继续开发
- ✅ 或者发布 Alpha 版收集反馈

---

## 📚 重要文档

### 必读
- **`快速测试指南.md`** - 详细测试步骤（本目录）
- **`当前开发状态.md`** - 进度总结（本目录）

### 参考
- **`DOC/PROGRESS.md`** - 开发进度
- **`DOC/SDK-INTEGRATION.md`** - SDK 集成说明
- **`DOC/plan.md`** - 完整实施计划

---

## 🎯 最重要的测试

**验证 Cursor AI 是否生成 Markdown 文件！**

如果 AI 生成的是：
- ✅ `01-scene.md` → 成功！
- ❌ `01-scene.json` → 需要调整 .cursorrules

这是整个项目最关键的验证点！

---

**现在，按 F5 开始测试吧！** 🚀

