# 生成失败问题调试指南

## 🔍 如何查看详细错误信息

### 1. 查看错误弹窗
生成失败时会显示错误弹窗，包含具体的错误信息，例如：
```
生成失败 [01-opening]: 文生图失败 [401]: Invalid API Key
```

### 2. 查看调试控制台（最重要！）

#### 在扩展开发主机窗口中：
```
帮助 → 切换开发人员工具
```

或按 `Ctrl+Shift+I`

#### 查看 Console 标签页
会看到详细的日志：
```
[API] 文生图请求: { url: '...', prompt: '...', size: '1280*720' }
[API] 文生图响应: { status: 401, ok: false, code: 'InvalidApiKey', message: '...' }
生成首帧失败: 01-opening 文生图失败 [401]: ...
```

### 3. 查看输出通道

在扩展开发主机窗口：
```
查看 → 输出
选择 "Vibe Video" 通道
```

---

## 🐛 常见错误及解决方案

### 错误 1: API Key 未配置
```
错误信息：请先配置视频 AI
```

**解决方案**：
1. 按 `Ctrl+,` 打开设置
2. 搜索 `vibevideo`
3. 填写 **Dashscope: Api Key**
4. 保存后重试

---

### 错误 2: API Key 无效
```
错误信息：文生图失败 [401]: Invalid API Key
或：Unauthorized
```

**原因**：
- API Key 输入错误
- API Key 已过期或被禁用
- 权限不足

**解决方案**：
1. 检查 API Key 格式（应该是 `sk-` 开头）
2. 访问 https://bailian.console.aliyun.com/ 验证 Key 是否有效
3. 确认是否开通了通义万相服务
4. 重新创建 API Key
5. 在设置中更新

---

### 错误 3: 余额不足
```
错误信息：Insufficient balance
或：Quota exceeded
```

**解决方案**：
1. 访问 DashScope 控制台
2. 查看剩余额度
3. 充值或购买资源包

---

### 错误 4: 网络问题
```
错误信息：fetch failed
或：ECONNREFUSED
或：timeout
```

**解决方案**：
1. 检查网络连接
2. 确认能访问 https://dashscope.aliyuncs.com
3. 检查防火墙设置
4. 如果在公司网络，可能需要配置代理

---

### 错误 5: 分镜格式问题
```
错误信息：缺少 firstFramePrompt
```

**解决方案**：
确保分镜 Markdown 包含：
```markdown
# 场景标题

- **生成首帧**: 人物/场景描述

正文...
```

---

## 🔍 详细调试步骤

### Step 1: 检查配置

运行命令：
```
Ctrl+Shift+P → "Vibe Video: Show Current Config"
```

应该看到：
```
当前 Provider: tongyi-wanxiang
状态: ✓ 已配置
API Key: sk-xxxxx...
```

如果显示"✗ 未配置"，先配置 API Key。

---

### Step 2: 检查分镜格式

打开一个分镜文件（如 `storyboards/01-opening.md`），确认包含：
```markdown
# 标题

- **生成首帧**: 这里是描述

正文描述...
```

关键：必须有 `生成首帧:` 或 `生成首帧：` 这一行

---

### Step 3: 单独测试一个分镜

1. 只保留一个包含 `生成首帧` 的分镜
2. 运行 `Generate First Frames`
3. 仔细查看错误信息

---

### Step 4: 查看完整日志

在原始窗口（vibevideo 项目）的**调试控制台**中：
```
查看 → 调试控制台
```

会看到：
```
[通义万相] 文生图请求: { prompt: '...', size: '...' }
[API] 文生图请求: { url: '...', prompt: '...', size: '...' }
[API] 文生图响应: { status: 401, ok: false, code: '...', message: '...' }
```

**关键信息**：
- `status`: HTTP 状态码
- `code`: 错误代码
- `message`: 错误描述

---

## 🛠️ 测试 API Key

### 手动测试（使用 curl）

在终端中运行：
```bash
curl -X POST https://dashscope.aliyuncs.com/api/v1/services/aigc/text2image/image-synthesis \
  -H 'X-DashScope-Async: enable' \
  -H "Authorization: Bearer 你的API_KEY" \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "wan2.5-t2i-preview",
    "input": {
      "prompt": "测试图片"
    },
    "parameters": {
      "size": "512*512",
      "n": 1
    }
  }'
```

如果成功，会返回：
```json
{
  "output": {
    "task_id": "xxx"
  },
  "request_id": "xxx"
}
```

如果失败，会返回错误信息。

---

## 📋 错误信息收集清单

请提供以下信息以便调试：

```
□ 错误弹窗显示的完整信息
□ 调试控制台的 [API] 日志
□ HTTP 状态码（如 401, 403, 500）
□ 错误代码（如 InvalidApiKey）
□ API Key 格式（前8位，如 sk-xxxxx）
□ 分镜文件内容（是否包含"生成首帧"）
□ 网络是否正常
```

---

## 🎯 最可能的原因

根据经验，90% 的失败是以下原因：

### 1. API Key 未配置或错误（80%）
```
检查：Ctrl+, → 搜索 vibevideo → 查看 API Key
解决：重新输入正确的 API Key
```

### 2. API Key 权限问题（10%）
```
检查：访问 DashScope 控制台，确认 Key 有效
解决：重新创建 API Key，确保有通义万相权限
```

### 3. 分镜格式问题（5%）
```
检查：打开分镜文件，确认有"生成首帧"
解决：手动添加或让 AI 重新生成
```

### 4. 网络问题（5%）
```
检查：curl 测试是否能连接
解决：检查网络、代理、防火墙
```

---

## 🚀 快速修复步骤

### 1. 验证 API Key
```
Ctrl+, → vibevideo → 检查 Dashscope: Api Key 是否填写
```

### 2. 测试连接
```
用 curl 测试（见上面的命令）
```

### 3. 查看详细错误
```
Ctrl+Shift+I → Console 标签 → 查找 [API] 日志
```

### 4. 根据错误码修复
- 401/403 → API Key 问题
- 400 → 参数问题（检查分镜格式）
- 429 → 请求太频繁，稍后重试
- 500 → 服务器问题，稍后重试

---

## 💡 请提供错误信息

请告诉我：

1. **错误弹窗显示什么？**
   - 完整的错误消息

2. **调试控制台的日志？**
   - 特别是 `[API] 文生图响应` 这一行

3. **API Key 配置了吗？**
   - 运行 `Show Current Config` 查看

4. **分镜格式正确吗？**
   - 打开一个分镜，复制内容给我看

有了这些信息，我就能精确定位问题！🎯

